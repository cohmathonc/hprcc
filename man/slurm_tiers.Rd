% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/targets_resource_shortcuts.R
\name{slurm_tiers}
\alias{slurm_tiers}
\title{List The SLURM Resource Tiers}
\usage{
slurm_tiers(tier = NULL, min_memory_gb = NULL)
}
\arguments{
\item{tier}{Optional tier name(s) to return, e.g. \code{"large_mem"}.}

\item{min_memory_gb}{Optional. Return only tiers with at least this much
memory, smallest first - "which tier can hold this?".}
}
\value{
A data frame with columns \code{tier}, \code{cpus}, \code{memory_gb}, \code{time_min},
\code{partition}, \code{gpu}.
}
\description{
Returns the resource tiers as a data frame, read from the live controllers,
so the values are the ones actually in effect on this cluster rather than
what the documentation says (#31).
}
\examples{
\dontrun{
slurm_tiers()
slurm_tiers("large_mem")
slurm_tiers(min_memory_gb = 150)
}
}
\seealso{
\link{SLURM-Resource-Configurations}
}
