% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slurm_dispatch.R
\name{get_result_path}
\alias{get_result_path}
\title{Get Result Path from SLURM Job Result}
\usage{
get_result_path(x, wait = TRUE, poll_interval = 30, timeout = 7200)
}
\arguments{
\item{x}{A \link{slurm_job_result} or character path.}

\item{wait}{Logical. If \code{TRUE} (default), polls SLURM until job completes.}

\item{poll_interval}{Numeric. Seconds between status checks (default 30).}

\item{timeout}{Numeric. Maximum seconds to wait (default 7200 = 2 hours).}
}
\value{
Character path to the completion file or script.
}
\description{
Extracts the output path from a \link{slurm_job_result}, optionally blocking
until the SLURM job completes. Used as a barrier in targets pipelines.
}
\section{Barrier pattern in targets}{


\if{html}{\out{<div class="sourceCode r">}}\preformatted{tar_target(
  job_ready,
  get_result_path(job_result, wait = TRUE),
  deployment = "main"
)
}\if{html}{\out{</div>}}
}

\seealso{
\code{\link[=dispatch_with_deps]{dispatch_with_deps()}}, \link{slurm_job_result}
}
