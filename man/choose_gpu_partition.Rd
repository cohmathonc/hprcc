% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slurm_gpu.R
\name{choose_gpu_partition}
\alias{choose_gpu_partition}
\title{Choose Best GPU Partition}
\usage{
choose_gpu_partition()
}
\value{
Character string: \code{"gpu-a100"} or \code{"gpu-v100"}.
}
\description{
Dynamically selects between \code{gpu-a100} and \code{gpu-v100} partitions based on
current queue backlog and node availability. A100 GPUs are ~2.5x faster
than V100 for ML workloads (e.g., CellBender), so A100 is preferred unless
the V100 queue is significantly shorter.
}
\details{
The selection algorithm:
\enumerate{
\item Query \code{sinfo} for available/mixed nodes per partition
\item Query \code{squeue} for pending jobs per partition
\item Compute queue density (pending jobs / available nodes)
\item Prefer A100 if its effective wait (density / 2.5) <= V100 density * 1.2
}

Falls back to \code{"gpu-a100"} if partition info cannot be queried.
}
\note{
Only available on Gemini cluster. Apollo does not have GPUs.
}
