% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{work_dir}
\alias{work_dir}
\title{Cluster working-data root}
\usage{
work_dir(...)
}
\arguments{
\item{...}{Optional path components appended below the root, via \code{\link[=file.path]{file.path()}}.}
}
\value{
Absolute path (character).
}
\description{
Where bulk working data lives on the current cluster. Deliberately \strong{not}
called \code{scratch_dir()}, because the two clusters do not share that concept:
}
\details{
\itemize{
\item gemini - \verb{/scratch/\{user\}}, genuinely per-user scratch
\item apollo - a shared lab directory with no per-user component
}

A function promising "per-user scratch" would therefore be wrong on apollo.
Callers wanting a project path should build on this, e.g.
\code{file.path(work_dir(), "myproject", "_targets")}.

Resolution order: \code{hprcc.work_dir} option, then \code{HPRCC_WORK_DIR}
environment variable, then the cluster default.
}
\examples{
\dontrun{
work_dir()
work_dir("myproject", "_targets")
}
}
