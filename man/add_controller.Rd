% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{add_controller}
\alias{add_controller}
\title{Add a Custom Controller to the Existing Controller Group}
\usage{
add_controller(
  name,
  slurm_cpus,
  slurm_mem_gigabytes,
  slurm_walltime_minutes = 720L,
  slurm_partition = default_partition()
)
}
\arguments{
\item{name}{A unique identifier for the controller. Must not conflict with
existing controller names.}

\item{slurm_cpus}{Number of CPU cores allocated to each task.}

\item{slurm_mem_gigabytes}{Memory allocated to each task, in gigabytes.}

\item{slurm_walltime_minutes}{Maximum allowed execution time per task, in
minutes. Defaults to 720 (12 hours).}

\item{slurm_partition}{SLURM partition for job submission. Defaults to the
cluster's default partition. See \link[=package-options]{package options}.}
}
\value{
Invisibly returns a \code{tar_resources} object for the new controller,
which can be used directly in \code{\link[targets:tar_target]{targets::tar_target()}} via the \code{resources}
argument.
}
\description{
Creates a custom SLURM controller and adds it to the existing
\link[crew:crew_controller_group]{crew controller group}. This simplifies adding
ad-hoc controllers for jobs with specific resource requirements that don't
match the pre-defined controllers (tiny, small, medium, large, etc.).
}
\details{
This function is useful when a project requires custom SLURM resource
configurations beyond the pre-defined controllers. For example, a job might
need more walltime but less memory than the standard \code{large_mem} controller.

The function modifies the targets controller group in place by calling
\code{\link[targets:tar_option_set]{targets::tar_option_set()}}. It must be called after loading the hprcc
package (which sets up the default controller group) and before defining
targets that use the custom controller.
}
\examples{
\dontrun{
library(hprcc)
library(targets)

# Add a custom controller for long-running jobs
singler_resources <- add_controller(
    name = "singler",
    slurm_cpus = 16L,
    slurm_mem_gigabytes = 200L,
    slurm_walltime_minutes = 720L,
    slurm_partition = "bigmem"
)

# Use in a target
tar_target(
    annotated_cells,
    annotate_with_singler(sce),
    resources = singler_resources
)
}
}
\seealso{
\link{SLURM-Resource-Configurations} for pre-defined resource shortcuts.
}
