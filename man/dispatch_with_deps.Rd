% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slurm_dispatch.R
\name{dispatch_with_deps}
\alias{dispatch_with_deps}
\title{Dispatch Job with SLURM Dependencies}
\usage{
dispatch_with_deps(fn, upstream, ...)
}
\arguments{
\item{fn}{Function that calls \code{\link[=run_slurm_job]{run_slurm_job()}} or \code{\link[=run_singularity_job]{run_singularity_job()}}.}

\item{upstream}{A \link{slurm_job_result} or list of them from an upstream target.}

\item{...}{Additional arguments passed to \code{fn}.}
}
\value{
A \link{slurm_job_result} from \code{fn}.
}
\description{
Wraps a processing function to handle SLURM job dependencies automatically.
Inspects upstream \link{slurm_job_result} objects, errors on failed jobs, and
injects \code{--dependency=afterok:JOBID} for jobs still running.
}
\details{
The dispatch flow:
\enumerate{
\item Extract job IDs from upstream results
\item Check status of each upstream job
\item Error immediately if any upstream failed/cancelled/timed out
\item Verify completion files exist for completed jobs
\item For pending/running jobs, set \code{options(slurm_job_dependency = "afterok:JOBID")}
which \code{\link[=run_slurm_job]{run_slurm_job()}} reads to inject \verb{#SBATCH --dependency=} headers
\item Call \code{fn(get_result_path(upstream, wait = FALSE), ...)}
}

This enables the entire SLURM DAG to be submitted in one \code{tar_make()} call
without blocking. SLURM's own dependency system handles sequencing.
}
\section{Usage in targets}{


\if{html}{\out{<div class="sourceCode r">}}\preformatted{tar_target(
  cellbender_result,
  dispatch_with_deps(run_cellbender, upstream = cellranger_result, ...),
  deployment = "main",
  cue = tar_cue(mode = "always")
)
}\if{html}{\out{</div>}}
}

\seealso{
\code{\link[=get_result_path]{get_result_path()}}, \code{\link[=run_slurm_job]{run_slurm_job()}}, \link{slurm_job_result}
}
