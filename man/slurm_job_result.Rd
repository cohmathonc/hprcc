% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slurm_job_result.R
\name{slurm_job_result}
\alias{slurm_job_result}
\title{Create a SLURM Job Result Object}
\usage{
slurm_job_result(
  path,
  job_id = NULL,
  status = "complete",
  working_dir = dirname(path),
  completion_files = NULL
)
}
\arguments{
\item{path}{Character. Path to completion file (if done) or script (if pending).}

\item{job_id}{Character or NULL. SLURM job ID (NULL if job already complete).}

\item{status}{Character. One of "submitted", "complete", "already_submitted".}

\item{working_dir}{Character. Working directory for the job.}

\item{completion_files}{Character vector or NULL. Expected output files that
signal successful completion.}
}
\value{
An S3 object of class \code{slurm_job_result}.
}
\description{
S3 class that tracks SLURM job submission state for targets integration.
Stores the path to the output/script, job ID, status, working directory,
and expected completion files.
}
\details{
The fire-and-track pattern uses this class to pass job state through targets:
\itemize{
\item \code{"complete"}: Completion file exists, job is done
\item \code{"submitted"}: Job was just submitted via sbatch
\item \code{"already_submitted"}: Job was previously submitted and is still running
}

Barrier targets use \code{\link[=get_result_path]{get_result_path()}} to block until the job completes.
Dependency injection uses \code{\link[=dispatch_with_deps]{dispatch_with_deps()}} to chain SLURM jobs.
}
\seealso{
\code{\link[=run_slurm_job]{run_slurm_job()}}, \code{\link[=get_result_path]{get_result_path()}}, \code{\link[=dispatch_with_deps]{dispatch_with_deps()}}
}
